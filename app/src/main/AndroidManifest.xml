<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.limpiezas.neteo">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".ui.App">
        <activity android:name=".ui.inicio.InicioActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.conocenos.ConocenosActivity"
            android:label="@string/conocenos_title"
            android:launchMode="singleTask"/>
        <activity android:name=".ui.eco.EcoActivity"
            android:label="@string/eco_title"
            android:launchMode="singleTask"/>
        <activity android:name=".ui.servicios.ServiciosActivity"
            android:label="@string/servicios_title"
            android:launchMode="singleTask"/>
        <activity android:name=".ui.reservas.Reservas2Activity"
            android:label="@string/formulario_title"
            android:launchMode="singleTask"/>
        <activity android:name=".ui.galeria.GaleriaActivity"
            android:label="@string/galeria_title"
            android:launchMode="singleTask"/>



    </application>

</manifest>